module.exports = {
    format_date: (date) => {
      // Format date as MM/DD/YYYY
      return date.toLocaleDateString();
    },

    // compare_user: (x, y) => {
    //  return x === y
    // }
  };

